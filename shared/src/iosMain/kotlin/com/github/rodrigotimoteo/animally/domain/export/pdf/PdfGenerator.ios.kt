package com.github.rodrigotimoteo.animally.domain.export.pdf

/**
 * iOS stub for the POC — a CoreGraphics-based renderer is deferred per
 * ADR-0013. Returns an empty payload so callers compile unchanged.
 */
actual fun generatePdf(report: PdfReportData): ByteArray = ByteArray(0)
